# Local development broker only — no auth, no TLS. Do not use in any
# environment where the port is reachable from outside the host.
listener 1883
allow_anonymous true

listener 9001
protocol websockets

persistence false
log_dest stdout
